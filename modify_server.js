const express = require('express');\nconst WebSocket = require('ws');\n\n// User credentials\nconst USERNAME = 'jonathanbackmanager';\nconst PASSWORD = 'jhonmasteroftheuniverseandgodofthedevelopment101001';\n\n// Initialize an Express application\nconst app = express();\n\n// Create a WebSocket server\nconst wss = new WebSocket.Server({ port: 8081 });\n\nwss.on('connection', (ws) => {\n  ws.on('message', (message) => {\n    const { username, password } = JSON.parse(message);\n\n    if (username === USERNAME && password === PASSWORD) {\n      ws.send('Authentication successful. Domain: websocket');\n    } else {\n      ws.send('Authentication failed. Invalid username or password.');\n    }\n  });\n});\n\n// Start the HTTP server\nconst PORT = process.env.PORT || 3000;\napp.listen(PORT, () => {\n  console.log();\n});
